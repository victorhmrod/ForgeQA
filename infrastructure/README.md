# Infrastructure

Reserved for future infrastructure-as-code (Terraform/Bicep/etc.) and deployment configuration.
Not used in Milestone 0 — local development and CI rely on `docker-compose.yml` and
`.github/workflows/ci.yml` at the repository root.

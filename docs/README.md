# Docs

- [Build Registry](build-registry.md) — Milestone 1: the `Build` domain entity, its API, and the
  scope boundary with Milestone 2 (Build Distribution).

For general setup and the overall roadmap, see the repository root [README.md](../README.md).
